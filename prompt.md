in feature/require-verified-publishers assicurati che 'In theory, private and local Snaps aren't (and shouldn't be; at most, we can add an additional check for private packages, as we did for local ones) affected by this change at all, since it only concerns public content in the community. ' e che 'If the configuration state is lost, it defaults to false. So nothing should happen here either, but we can perform additional checks on this as well.' Quindi se ci sono vulnerabilità o inconformità , correggi alla radice e pusha (1 riga msg in caso)